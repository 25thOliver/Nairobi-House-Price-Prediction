# Minimal dependencies for Streamlit app + Dashboard (Day 5 & 6).
# Use this file on Streamlit Community Cloud to avoid building lxml (needs system libs).
# For local scraping, use requirements.txt instead.

# Data & ML
pandas>=2.0,<3
numpy>=1.24,<2
scikit-learn>=1.3
xgboost>=2.0

# Visualization (Dashboard)
matplotlib>=3.7

# Web App
streamlit>=1.28
